# @w8-ui/prettier-config

## Установка

```bash
npm install -D @w8-ui/prettier-config prettier
```

## Использование

Добавьте файл `.prettierrc.js` в корень проекта с следующим содержимым:

```js
module.exports = require('@w8-ui/prettier-config');
```
