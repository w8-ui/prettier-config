# @w8-ui/prettier-config

## How to install ??

```bash
# or pnpm or yarn
npm install -D @w8-ui/prettier-config prettier
```

## Usage ??

Add `.prettierrc.js|ts` file in the project root with the following content:

```js
import { baseConfig } from "@w8-ui/prettier-config";
```
